// Funciones del DOM.

// Si se selecciona un Elemento en el inspector es $0.

// let li = document.createElement("li");

// $0.prepend(li); o $0.before(li); o $0.append(li); o $0.after(li);

// $0.remove.(li);